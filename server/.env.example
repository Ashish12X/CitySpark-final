MONGODB_URI=mongodb://127.0.0.1:27017/cityspark
PORT=5000
# Required in production (use a long random string, e.g. openssl rand -hex 32)
JWT_SECRET=
JWT_EXPIRES_IN=7d
NODE_ENV=development
# Optional but recommended for dynamic AI guidance (PAN/Aadhaar/cards/ration/etc.)
GEMINI_API_KEY=
